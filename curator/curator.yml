elasticsearch:
  client:
    hosts: http://${ELASTIC_GRAFANA_HOST}:${ELASTIC_GRAFANA_PORT}
    cloud_id:
    bearer_auth:
    opaque_id:
    request_timeout: 60
    http_compress:
    verify_certs:
    ca_certs:
    client_cert:
    client_key:
    ssl_assert_hostname:
    ssl_assert_fingerprint:
    ssl_version:
  other_settings:
    master_only:
    skip_version_test:
    username: ${ELASTIC_GRAFANA_USERNAME}
    password: ${ELASTIC_GRAFANA_PASSWORD}
    api_key:
      id:
      api_key:

logging:
  loglevel: INFO
  logfile:
  logformat:
  blacklist: []